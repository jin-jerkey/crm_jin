import { BackAccountFormatPipe } from './back-account-format.pipe';

describe('BackAccountFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BackAccountFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
